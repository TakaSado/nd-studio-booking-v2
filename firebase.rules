rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 初期は極めて厳格（原則全拒否）
    match /{document=**} {
      allow read: if false;
      allow write: if false;
    }
  }
}

